[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pytest-hypothesis-corpus"
version = "0.1.0"

[project.entry-points.pytest11]
hypothesis_corpus = "plugin"
