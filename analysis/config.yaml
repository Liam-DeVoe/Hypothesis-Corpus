# Configuration for PBT corpus analysis

database:
  path: "data/analysis.db"

docker:
  image: "pbt-analysis:latest"
  timeout: 300  # seconds per test
  memory_limit: "2g"

workers:
  max_workers: 4
  queue_size: 100
